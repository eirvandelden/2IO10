%% Heroefening ``Wat een werk'', Opdracht 4

\item Hoelang duurden de klussen gemiddeld? \\

%\emph{Interpretatie:}  \\
%\emph{Aanname:}  \\

%1: \emph{Antwoord:} De klussen duurden gemiddeld: $\textbf{avg}(~\forall_{l} [~ l \in K : \textbf{TRUE}: e(l) - b(l) ])$ \\
%1: \emph{Assessment:}  Fout: gebruik van de (ongedefinieerde) functie avg is zo niet toegestaan.\\

\emph{Antwoord:} $( (+ l \in \mathcal{L}, k \in \mathcal{K}: n(l) = k : e(l) - b(l)) / (+ l \in \mathcal{L}, k\in \mathcal{K}: n(l) = k : 1) )$ \\
\emph{Assessment:} Dit kan veel korter.

\emph{Verbeterd antwoord:} $ (+ ~ l \in \mathcal{L} :~: e(l) - b(l)) ~/~ \#\mathcal{L}$ \\
%\emph{Redenering:}  \\
